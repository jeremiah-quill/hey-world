export const defaultHtml = `<h1>hey world.</h1>`;
export const defaultCss = `body {
  display: grid;
  place-items: center;
  font-family: system-ui;
  background: #292524;
}

h1 { 
  color: #fff; 
  margin: 0px;
  font-weight: 900;
}`;
